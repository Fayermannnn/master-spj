# Domain: Shared

Value object, enum, interface, trait lintas-domain yang tidak spesifik satu domain (mis. Money, DateRange).

> Lihat `docs/MODULES.md` dan `docs/domain-map.md` untuk aturan impor lintas domain.
